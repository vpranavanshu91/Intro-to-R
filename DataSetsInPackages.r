require(ggplot2)
data(diamonds)      #Diamonds DATA present inside ggplot2 is loaded
head(diamonds)      #Display Heads of Diamonds table

data("tips",package = "reshape2")   #Tips data presnt in reshape2 package loaded without the package being loaded
head(tips)

data()    #displays all the data's available in a separate tab
